# VAIB System (Verified AI Builder) v16.3: The "No-Magic" Industrial Pipeline

> *"Не лечи симптомы — ищи причину. Не надейся на AGI — построй конвейер."*

---

## ❓ Что это такое?

**VAIB SYSTEM** — это **набор конфигураций для AI-агентов** (файл `.kilocodemodes`), который превращает вашу IDE (Kilo Code) в настоящий завод по производству софта.

Обычно вы общаетесь с одним "умным" чат-ботом, который пытается делать всё сразу: и код писать, и архитектуру думать, и тесты гонять. В итоге он путается и выдает мусор.

**VAIB делает иначе.** Он разбивает этого "одного бота" на **8 узкоспециализированных сотрудников**, каждый из которых имеет свою инструкцию, свою роль и свои ограничения. Они не спорят, не импровизируют и передают работу друг другу по конвейеру.

### 🛠 Где это работает?

- **Платформа:** Kilo Code (или любая IDE, поддерживающая формат `.kilocodemodes` / Custom Modes).
- **Языки:** Python, JavaScript/TypeScript, Go (и любые другие — агенты агностичны к языку).
- **Проекты:** От новых пет-проектов до сурового Legacy-кода.

---

## 💀 Проблема: "Вот выйдет модель поумнее и всё само заработает"

Мы все живем в ожидании Той Самой Модели, которая будет обладать бесконечным контекстом, никогда не галлюцинировать и писать идеальный код с первого раза.

**Спойлер: Не будет.**

Даже самая умная модель начинает "плыть", когда вы просите её одновременно быть архитектором, кодером, тестировщиком и специалистом по безопасности. Она перегружается, теряет фокус и начинает выдумывать API.

**VAIB SYSTEM** снимает нагрузку с "интеллекта" и перекладывает её на **процесс**. Мы нарезаем задачу на такие тонкие ломтики, что накосячить в них сложнее, чем сделать правильно. А поскольку задачи изолированы, вы можете использовать разные модели: "умных мыслителей" для архитектуры и "быстрых исполнителей" для написания кода.

---

## 🏗️ Детальный Пайплайн (Кто, Что, Куда)

Система работает как эстафета. Каждый агент получает артефакт от предыдущего, обрабатывает его и передает дальше.

| Стадия | Агент | Входные данные | Действие | Выходной Артефакт |
| :--- | :--- | :--- | :--- | :--- |
| **0** | **Archaeologist** | `src/*.py` (Legacy) | Читает код, понимает суть, вставляет семантические метки (`# START_CONTRACT`), не ломая логику. | Размеченный Codebase, готовый к изменениям. |
| **1** | **Analyst** | Ваш запрос в чате | Задает уточняющие вопросы (AAG Model). Формализует "хотелки" в документ. | `vaib/01-analyst/requirements.md` |
| **2** | **Architect** | `requirements.md` | Проектирует структуру классов. Для сложной логики пишет **Псевдокод** с мат. доказательством (PRE/POST). | `vaib/02-architect/development_plan.md`<br>`vaib/02-architect/technology.md` |
| **3** | **Spec** | `technology.md` | Проверяет библиотеки на CVE/Deprecated. Скачивает **реальную** документацию, чтобы Кодер не галлюцинировал методами. | `vaib/docs/*.md` (Context Knowledge) |
| **4** | **Coder** | `development_plan.md`<br>`docs/` | **Тупо пишет код.** Переводит псевдокод в Python/JS. Вставляет логи намерений (`Belief Logs`). | `src/new_module.py` (Draft Implementation) |
| **5** | **Tester** | `src/`<br>`rules.md` | Пишет тесты (если их нет). Запускает код. Сверяет `Belief Logs` с контрактами. Если баг — возвращает Кодеру. | ✅ Verified Code<br>❌ Error Report |
| **6** | **Editor** | Error Report | Исправляет мелкие синтаксические ошибки или опечатки, не трогая логику. | Hotfix |
| **7** | **Expert** | Deadlock Loop | Вызывается, если Coder и Tester гоняют баг по кругу 3 раза. Проводит расследование (RCA). | `Research.md` (План спасения) |

---

## 🎮 Полный Контроль (Human-in-the-Loop)

Главная фишка VAIB — **всё состояние хранится в файлах, а не в контексте чата.** Это дает вам суперсилу: вы можете вмешаться в процесс на любой стадии.

### Кейс: "А давайте всё переиграем?"

Представьте: вы прошли Аналитика и Архитектора, получили план консольной утилиты. И тут вас озарило:

> *"Слушай, а давай сделаем из этого десктопное приложение на Electron?"*

В обычном чате с GPT вам пришлось бы начинать всё с нуля, объясняя контекст заново.

**В VAIB System:**

1. Вы открываете `vaib/01-analyst/requirements.md` и сами (или через Аналитика) меняете одну строчку: `Interface: CLI` -> `Interface: GUI (Electron)`.
2. Запускаете Архитектора (`@Vaib2 Architect`). Он видит новые требования и переписывает `development_plan.md` и `technology.md`.
3. Запускаете Спека (`@Vaib3 Spec`). Он видит `Electron` в стеке, идет качать документацию по Electron (вместо `argparse`).
4. Запускаете Кодера.

**Пайплайн перезапускается с середины.** Ничего не потеряно. Контекст сохранен в файлах. Вы управляете процессом, редактируя markdown-файлы, как конфигурацию сервера.

---

## 📜 Почему это работает (Anti-Hallucination Design)

Когда у модели есть **четкий контракт на вход** и **четкий шаблон на выход**, у неё **нет шансов накосячить**.

### 1. Изоляция Контекста

Кодеру не нужно знать о "бизнес-целях заказчика". Ему нужно знать только: "Вот функция, вот входные данные, вот формула". Мы отсекаем 90% шума, который обычно сбивает модели с толку.

### 2. Принудительная Дисциплина (GRACE Markup)

Мы заставляем агентов размечать код специальными тегами:

```python
# START_CONTRACT_CALC_TAX
""" 
    Намерение: Рассчитать НДС. 
    Вход: float. 
    Выход: float. 
"""
# END_CONTRACT_CALC_TAX
```

Если Кодер написал код, который противоречит его же контракту, Тестер (Stage 5) это увидит механически, сравнив `[Belief Log]` с реальностью.

### 3. Бюрократия вместо Магии

Все правила записаны в `vaib/rules.md`:

- **Лимит цикла исправлений?** 3 раза.
- **Перевод легаси комментариев?** Запрещено.
- **Формат логов?** Строгий шаблон.

Модели не нужно "догадываться" о стиле проекта. Она просто выполняет инструкцию.

---

## 📂 Структура Файлов

Система сама создает и поддерживает эту структуру. Не удаляйте папку `vaib/`.

```text
.
├── .kilocodemodes       # Конфигурация агентов (Мозг)
├── src/                 # Ваш код (с GRACE разметкой)
├── tests/               # Тесты (генерируются Tester'ом)
└── vaib/                # Память системы
    ├── rules.md         # КОНСТИТУЦИЯ ПРОЕКТА (Read-only)
    ├── 01-analyst/
    │   └── requirements.md  # ЧТО мы делаем
    ├── 02-architect/
    │   ├── development_plan.md  # КАК мы делаем
    │   └── technology.md        # ЧЕМ мы делаем
    └── docs/            # Доки, скачанные Spec'ом
```

---

## 🚀 Как использовать

### Установка

Положите файл `.kilocodemodes` (настройки агентов) и `vaib/rules.md` (правила) в корень вашего проекта.

### Перезапуск IDE

Kilo Code подхватит новые режимы.

### Onboard

```
@Vaib0 Archaeologist: Разметь мне src/main.py
```

### Задача

```
@Vaib1 Analyst: Хочу систему расчета налогов.
```

### Поток

```
Analyst -> Architect -> Spec -> Coder -> Tester
```

Вы наблюдаете, как задача перетекает из "идеи" в "проверенный код".

---

## ⚠️ Предупреждение

Если вы ищете кнопку "Сделать Хорошо", которая понимает вас с полуслова — это не она.

**VAIB SYSTEM** — это экзоскелет для разработчика, а не замена ему. Она заставит вас утверждать планы и читать требования.

Зато вы перестанете дебажить галлюцинации. И ваш проект через месяц не превратится в тыкву.

---

> **v16.3 Hybrid Edition (2026)**
> *"У моделей нет шансов на ошибку, если у них нет права на фантазию."*
